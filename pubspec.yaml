name: gks_rider
description: "GKS Rider — Delivery partner app for GKS backend"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  dio: ^5.4.0
  flutter_secure_storage: ^9.0.0
  provider: ^6.1.0
  socket_io_client: ^2.0.3+1
  intl: ^0.19.0
  go_router: ^14.0.0
  geolocator: ^11.0.0
  flutter_map: ^7.0.0
  latlong2: ^0.9.0
  url_launcher: ^6.2.0
  cached_network_image: ^3.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
